// Displays how a single candidate is doing in a single race
import React, { PropTypes } from 'react'
import _ from 'lodash'
import addCommas from 'add-commas'
import { percent } from './../utils/Candidate.js'
import { sortByIDs } from './../utils/Candidates.js'
import { percentForDisplay } from './../utils/standardize.js'

const RaceSummaryRow = ({ candidate }) => {
	return (
		<h1>hello</h1>
	)
}

/*RaceSummaryRow.propTypes = {
	candidate: PropTypes.object.isRequired,
}*/

export default RaceSummaryRow